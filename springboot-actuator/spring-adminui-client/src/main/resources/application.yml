## 配置client端，注册到admin ui平台
spring:
  boot:
    admin:
      client:
        url: http://localhost:8080
server:
  port: 8081

## 配置启用所有的监控端点，默认情况下，这些端点是禁用的
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: ALWAYS
